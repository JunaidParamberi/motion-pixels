# Draft project slot

1. Rename this folder to your URL slug (e.g. `acme-launch`).
2. Add files: `card.jpg` (or `.png`), `hero.jpg`, plus any `gallery-*` assets.
3. Copy the `draft-project` block in `case-study-data.ts`, change `slug` to match the folder name, update copy, and point image paths to `/images/projects/<your-slug>/...`.
4. Remove your slug from `disabledCaseStudySlugs` when the page should go live.
